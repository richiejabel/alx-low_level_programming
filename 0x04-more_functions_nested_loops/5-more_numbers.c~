#include "main.h"

/**
 * more_numbers - function that prints 10 times the numbers, from 0 to 14
 */

void more_numbers(void)

{
	int c;

	for (c = '0'; c <= 14; c++)

		_putchar(c);
	_putchar('\n');

	
	if (n <= 10)
		continue;
}
