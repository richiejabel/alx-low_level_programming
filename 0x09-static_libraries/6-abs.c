#include "main.h"

/**
 * _abs - absolute value of integer
 * int: integer
 * @num: number being checked
 * Return: Always 0
 */

int _abs(int num)
{
	if (num >= 0)

		return (num);

	else
		return (-num);
}
